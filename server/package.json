{
  "name": "server",
  "version": "0.0.1",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "hono": "^4.10.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
